on:
    push:
        branches:
            - master





jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3 

            - name: "Login to docker hub"
              run: docker login --username booda14 --password dckr_pat_jhSspsm5Eur-c-1UgqA8Shsd-9s
        
          